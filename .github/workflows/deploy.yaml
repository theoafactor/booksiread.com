name: "Simple Deploy for Books I Read"
on:
    push:
        branches:
            - development
            
jobs: 
    Development:
        runs-on: ubuntu-latest
        steps:
            - name: "Checkout"
              uses: actions/checkout@v4

            - name: "Generate Random number"
              run: |
                echo "APP_VERSION=${RANDOM}" >> GITHUB_ENV
                echo "REP_USERNAME=${{ secrets.DOCKERHUB_USERNAME }}" >> GITHUB_ENV

            - name: "Build the Docker Image"
              run: docker build -t ${{ env.REP_USERNAME }}/simpleweb:${{ env.APP_VERSION }} frontend/

            - name: "Login to Docker Hub"
              uses: docker/login-action@v3
              with: 
                username: ${{ secrets.DOCKERHUB_USERNAME }}
                password: ${{ secrets.DOCKERHUB_PASSWORD }}

            - name: "Push the Docker image built to Docker Hub"
              run: |
                docker push ${{ env.REP_USERNAME }}/simpleweb:${{ env.APP_VERSION }}
